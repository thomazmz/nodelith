// import { CoreContract } from "core-contract"

// export declare namespace CoreContractField {
//   export type Infer<C> = C extends CoreContractField<infer T> ? T : never
// }

// export interface CoreContractField<T extends CoreContract.FieldLiteral = any> {
//   parse(value: unknown, errorConstructor?: new (message: string) => Error): T
// }
