{
  "name": "@nodelith/root",
  "license": "MIT",
  "author": "Thomaz Zandonotto <thomaz.zandonotto@gmail.com>",
  "private": true,
  "keywords": [
    "typescript",
    "javascript",
    "monorepo",
    "toolkit"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/thomazmz/nodelith"
  },
  "scripts": {
    "build": "pnpm --recursive --if-present run build",
    "test": "pnpm --recursive --if-present run test",
    "test:coverage": "pnpm --recursive --if-present run test:coverage",
    "build:watch": "pnpm --parallel --if-present run build:watch",
    "test:watch": "pnpm --parallel --if-present run test:watch"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
